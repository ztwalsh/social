# Shot list — assistantOS Markdown → formatted Google Doc

For the build-log entry `34f7c73` (2026-09-08).

- **Before** — a Google Doc created by the old plain-text path: literal
  `# Heading`, `**bold**`, `| table | pipes |` sitting as raw text.
- **After** — the same source Markdown pushed again: real H1/H2, actual bold and
  italic, a rendered table, proper bullet and numbered lists.
- Side-by-side of the two Docs is the ideal single image.
- Optional: the "Push to Drive" control in assistantOS being clicked, for
  context on where this happens.
