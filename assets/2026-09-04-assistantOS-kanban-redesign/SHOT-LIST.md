# Shot list — Projects kanban redesign (assistantOS, 093445c)

- **Full board, before vs after** — the "assistantOS" project's kanban view
  (To Do / In Progress / Review / Done). Before: flat columns, plain "P0"
  text, no lane separation. After: muted swim-lane containers, icon + count
  per column header, colored priority dots.
- **Single card close-up** — one card showing the priority dot + label, the
  title, and the meta row with the timestamp and the new clickable "PR" link.
- **Hover state** — a card mid-hover showing the run/delete icons revealed
  and the subtle background tint + shadow lift.
