# Shot list — assistantOS "Copy as…" selection menu

For the build-log entry `c89e790, f5cb131, 7e9ea3c, 52676bb` (2026-09-10).

- **Right-click menu open** on a highlighted text selection in the rich
  markdown editor — showing Copy as Markdown / Rich Text / Plain Text.
- **Same menu in the read-only doc view**, to show it works in both places.
- **Before/after paste** — copy a selection with formatting (a heading, a bold
  word, a list item), then paste into a plain-text field. "Before" = the old
  behaviour or raw HTML; "after" = clean markdown.
- Optional: the project board column headers with the flat muted icons, next to
  an older screenshot with the coloured info/warning/success tint.
