# Shot list — app-wide motion pass (assistantOS)

- Screen recording: file viewer opening → toggle into edit mode (watch the
  panel replay its entrance) → close (entrance in reverse, quicker). ~4–6s.
- MOTION.md open to the token table (durations + easings), so the "it's a
  system, not one-off transitions" point is visible.
- Optional: a fast montage of 2–3 other screens' load-in/hover so it reads as
  "seven screens, one language" not just the file viewer.
