# Shot list — Spike C latency results

- Terminal screenshot of one `swift run SpikeCTranscribe` run, full output visible:
  the countdown, the transcript, and the `audio / transcribe / realtime / budget PASS` block.
- If possible, a composite of all three takes' stat blocks side by side to show consistency (18x–28x).
- Optional: a screenshot of the updated table in `spikes/README.md` for a "here's it documented" beat.
