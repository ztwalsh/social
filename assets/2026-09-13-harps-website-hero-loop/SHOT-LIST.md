# Shot list — hero field/capsule loop (d01eb51)

- Screen capture of the full hero loop, start to finish: empty field with blinking caret → capsule rises and listens (waveform) → transcribing state → capsule collapses as the result drops into the field → field dwells with the full sentence → text deletes itself character by character → loops back to empty
- Close-up of just the field↔capsule swap moment (the scaleX collapse/expand), slowed down if possible
- Static screenshot of each of the four states side by side: idle, listening, transcribing, text landed
